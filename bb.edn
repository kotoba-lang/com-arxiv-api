{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashaka"
               :extra-paths ["test"]
               :requires ([arxiv_api.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'arxiv_api.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
